# teamesyfo-github-actions-workflows

Read `README.md` for the reusable workflows, composite actions and their
contract documentation. For affected files, read the files in
`.github/instructions/` whose frontmatter `applyTo` patterns match those paths.
Keep the detailed guidance in those canonical files.
